-- AI related plugins

return {
  "yetone/avante.nvim",
  opts = {
    selection = { enabled = true, hint_display = "delayed" },
    behaviour = {
      auto_approve_tool_permissions = false,
    },
  },
}
